workflows:
  ios-workflow:
    name: iOS Build
    environment:
      flutter: 3.19.0
      xcode: latest
    scripts:
      - flutter pub get
      - flutter build ipa
    artifacts:
      - build/ios/ipa/*.ipa
